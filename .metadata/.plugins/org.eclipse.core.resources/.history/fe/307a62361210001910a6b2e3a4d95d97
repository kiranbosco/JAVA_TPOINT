package com.unray;

public class Unray {
	public static void main(String[] args) {
		
		int x =10;
		int y =20;
		x ++; // after x operator using increment only one 
	//	x --; // after x operator using decrement only one
	//	++  x; // actual x value and  add one more value
	//	--x; // actual x value and decress the one more value
		
	//	System.out.println(x++ + y++);
	//	System.out.println(x);

		System.out.println( 10 + 20 +"java point");
		System.out.println("java point " + 10 + 20);
		
/*		constructor is the block it can be using the create the object directly constructor should  be same as class name
		constructor should not be return any value even theoug void also
				
				there are two type of the constructo in java defauld construcot and parameter of the construcot
				: default costructor can ccreate the object same  value same place many time
				
				:parameterized constructor it will be create the object different value same place
	}

}*/
